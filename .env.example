# Domain this instance is served from (frontend + backend API on the same origin).
# Used server-side to build the API base URL, sitemap.txt/robots.txt links and the canonical <link>.
# Copy this file to .env for local dev (Nuxt loads .env automatically), or set the same
# variable in your deployment environment / docker-compose.yml.
API_HOST=
